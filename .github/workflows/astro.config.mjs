// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({});
  // Tu usuario de GitHub
  site: 'https://matrizindie.github.io',
  
  // Si el repo se llama distinto a "tu-usuario.github.io", pon el nombre aquí:
  base: '/matrizindie', 
});